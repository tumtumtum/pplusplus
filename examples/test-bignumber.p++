using "string.p++";
using "stream.p++";
using "bignumbers.p++":

function main()
{
	var x : biginteger;
	var y : biginteger;

	println("Type in the first number (make it huge):");

	x := readln();

	println("Type in the second number (make it huge):");

	y := readln();

	println("Answer:");
	println(x + y);
};